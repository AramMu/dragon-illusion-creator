#ifndef GEOMETRY_H
#define GEOMETRY_H

/*
class Geometry {
public:
    static double distance
private:
    Geometry();
    Geometry(const Geometry& e);
    Geometry& operator = (const Geometry& e);
};
*/

#endif // GEOMETRY_H
